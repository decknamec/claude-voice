# Whisper-Modelle (mehrere hundert MB) - holt install.sh
whisper-models/
*.bin
# Laufzeitkram
*.log
.DS_Store
# whisper-server legt bei --convert Bruchstuecke im CWD ab
whisper-server-*.wav
voice-enabled
voice-loop.lock
voice-locks/
.elevenlabs-voice
# Never commit keys - the real file lives in ~/.claude/ anyway
*.env
voice.env
# Personal configuration - the repo carries only the .example files
conf/voice.conf
conf/voice-loop.conf
node_modules/

# The React build is a result, not a source.
app/dist/
app/node_modules/
desktop/node_modules/
desktop/src-tauri/target/
desktop/src-tauri/gen/
app/tsconfig.tsbuildinfo
