# Whisper-Modelle (mehrere hundert MB) — holt install.sh
whisper-models/
*.bin
# Laufzeitkram
*.log
.DS_Store
# whisper-server legt bei --convert Bruchstuecke im CWD ab
whisper-server-*.wav
voice-enabled
voice-loop.lock
voice-locks/
.elevenlabs-voice
# Keys niemals ins Repo — die echte Datei liegt ohnehin in ~/.claude/
*.env
voice.env
# Persönliche Konfiguration — im Repo liegen nur die .example-Dateien
conf/voice.conf
conf/voice-loop.conf
node_modules/

# Der React-Bau ist ein Ergebnis, keine Quelle.
app/dist/
app/node_modules/
